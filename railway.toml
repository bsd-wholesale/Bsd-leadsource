[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[build.env]
NODE_VERSION = "20"

[build.nixpacks]
phases = {
  build = {
    cmds = ["npm run build"]
  },
  start = {
    cmds = ["npm start"]
  }
}
